# Tasks: steward-product-workflow

依赖：`skill-steward-contracts`、`skill-steward-runtime`、`dsh-runtime-integration`、`dsh-webui-composition` 完成并通过门禁。此 change 不再设计第二套独立 Agent shell。

- [x] 4.1 在已注册的 DSH client plugin 内实现任务与范围选择。
  - Files: `packages/skill-creator-dsh-client/`, `src/shared/rpc-contract.ts`, `webui/src/lib/stores/`.
  - Steps: 只实现 task/target/selected-skills/runtime-config stores 和对应 workflow view；复用阶段 4 已注册的 plugin/root/connection/RPC owner，不注册第二个 plugin、root、route owner 或 Agent shell。
  - Acceptance: scope/task/runtime config and run projection survive reconnect with latest-request-wins semantics inside the DSH host；plugin registration 仍只由 `dsh-webui-composition` 负责。
  - Evidence: 新 store `webui/src/lib/stores/steward-workflow.svelte.ts`（per-target 键控选择 `{taskKind, selectedSkillIds, instructions≤2000}` 模块级 $state——跨 island 卸载/重连存活、无 localStorage；`loadStewardRuntimeConfig`/`applyStewardRuntimeConfigPatch` 走 `dsh.settings.*`，typed rejected 原样返回；`startStewardWorkflowRun` 走 `skillSteward.startRun`，run 投影绑定 targetKey——提交要求 isCurrent、loading 清理只需 isLatest 的代次协议）+ 新视图 `StewardWorkflowView.svelte`（manifest 新 activity `workspaces.steward-workflow`，ProviderView 增 Workflow tab；无第二个 plugin/root/Agent shell）。单测 `webui/src/lib/__tests__/steward-workflow.test.ts` 6/6（键控/toggle 去重/instructions 钳制/慢请求不覆盖新 run/断线后回落响应零提交+新连接可提交/unavailable typed 错误/rejected 不动视图+迟到补丁丢弃）+ `route-match.test.ts` 6/6（全部 activity 路径匹配含 workflow 3 段路由、未知前缀不吞）。测试基建：root vitest 改 projects（webui 项目经 webui 安装的 vite-plugin-svelte 编译 runes；node 项目不变——root 管线无 svelte 插件，`.svelte.ts` 的 `$state`会变裸引用）。轮内真实缺陷修复：视图曾在`$derived.by` 内调用 `selectionFor` 初始化 $state 选择表 → Svelte `state_unsafe_mutation` 击穿叶子渲染（SPA 表现为路由 fallback）——改为 `$effect.pre` 初始化。浏览器取证（`scripts/dsh-release-evidence.sh.ts --hold` 真实生产组合宿主，artifacts/steward-workflow-4.1-island-*.png 4 张，0 JS 错误）：island 内 Workflow tab 真实技能多选（1 selected）+ Organize 切换 + `Run organize` 经 RPC 产出 run 投影（1 tool call、4 accepted/0 dropped、1 proposal split `spp_*`、completed）+ runtime config 补丁 approval ask→never 生效（rev 0 视图刷新）+ island 关闭重开后选择与 run 投影完整存活。门禁：454/454（54 files）、typecheck 0、webui check 0/0、build、fmt 482 clean、diff-check clean、openspec 9/9。
- [x] 4.2 实现 DSH-hosted 产品级 workflow UI。
  - Files: `packages/skill-creator-dsh-client/`, components and styles.
  - Acceptance: timeline, tool calls, evidence graph, diff, validation, approval, rollback and recovery states work at 1100px and 680px without overflow; no iframe or parallel shell.
  - Evidence: `StewardWorkflowView` 扩为完整提案工作流（同一 island/root，无 iframe/第二 shell）：每提案 validate→approve→apply→rollback 操作链（valid 才可 Approve、grant 才可 Apply、applied 才可 rollback；两类 rollback 形态——enablement reverse proposal 走 Approve reverse/Apply reverse、split/merge rollback grant 走 Rollback(replay)，以 `spp_`+16 零占位 id 区分）、mutation diff 表（relPath+semantic+before→after revision——Manager 修订级事实，不伪造内容 diff）、validation checks 逐项清单、recovery/compensated/rollback-failure 恢复横幅（含 daemon 侧 failure 文本）、append-only timeline（run-started→validated→approved→applied→rollback-prepared→rolled-back/error）、agent stream（dsh.sessions.streams 脱敏帧按当前 run session 过滤，tool-call/tool-result 即 tool calls 证据链）。生产 stream 数据源接线：`bootDaemon` 在 DSH host 挂载后 `setDshSessionBinder`（binder 增 collector 钩子把 turn/tool 事件喂进 dsh.settings 环形缓冲——此前生产无消费者，stream 恒空）。轮内修复三个真实缺陷：① store record 返回裸对象致 mutation 不触发 Svelte 信号（busy/validation 卡死）→ 统一返回 proxy；② `expectedJournalStepsOf` 零计数入表使 resource-less split/merge 的 rollback 双射必然失配（全部误报 recovery-required）→ 零计数不入表（runtime 测试 +1 回归）；③ darwin canonical 豁免只盖 /var 不盖 /tmp→/private/tmp（/tmp 短路径 sandbox 的 apply 全部 compensated）→ 豁免扩为 darwin 系统级 symlink 根（r8 probes +2）。单测：steward-workflow 11/11（新增双 rollback 形态/recovery 投影/stale 门/帧过滤）+ runtime 70/70 + r8 probes 20/20。浏览器取证（真实生产组合宿主，7 张 steward-workflow-4.2-_.png，0 JS 错误）：organize run→Validate(3/3 checks)→Approve(grant__)→Apply(applied + mutation diff 3 行 + 磁盘 split 落盘 exec/plan/.SKILL.md)→Prepare rollback→Rollback(replay)→磁盘恢复原状（find 证据）；agent stream 真实帧（dsh session-2：turn-start + tool-call skills.propose）；1100px/680px 容器强制宽度下 scrollWidth===clientWidth 无横向溢出。门禁：462/462（54 files）、typecheck 0、webui check 0/0、build、fmt 473 clean、diff-check clean、openspec 9/9。
- [x] 4.3 验收旧 ACP 入口已移除并补齐最终用户文档。
  - Files: `webui/src/lib/components/creator/`, `README.md`, shared route/contracts.
  - Acceptance: 复核阶段 4 的入口清理，无 generic ACP 产品入口；README 记录真实安装、模型配置、维护与恢复流程，所有命令在生产包实测。
  - Evidence: ACP 复核——`rg -i acp webui/src` 仅 3 行（CreatorWorkspace.svelte 头部/分区注释，全部为 3.2 移除决策记录）；README 3 处 ACP 提及均带 internal-legacy/移除标注（无产品叙事行数=0）。README 用户文档——新增「技能管家（Skill Steward）」章节（模型配置：deterministic/live preset、凭据 0600 私有文件与只显状态、approval ask/never 只投影运行时不放宽 apply 授权；维护流程：check/optimize/organize → 范围选择 → run → validate/approve/apply 操作链与 mutation diff → 两类 rollback 形态；恢复流程：DSH unavailable 降级 Manager-only + status.dsh 原因、recovery-required/compensated 终态语义与处理入口、断线重连存活、重启后未消费 grant 失效）+ intro/产品边界/arch 树（+dsh-host-lifecycle/steward/dsh-session-binder）+ RPC 表（+skillSteward/dsh namespace）同步真实实现。命令生产包实测（`pnpm build` 后的 `dist/`——与发布 tarball 同内容；隔离 HOME + headless（tray/windowed 路径由 4.1 证据持有））：`version`→2.0.0、`help`→用法、`start`→"daemon started…headlessly…openinbrowser 提示"exit 0、`status`→pid/端口/tray 终态/带 token URL、`open`→headless 提示 exit 0、`openinbrowser`→打印 URL 并调系统浏览器、`/api/health`→{"ok":true}、`/`→200、`stop`→endpoint 立即释放、复跑 `status`→ENOENT+start 恢复提示。复现事实：干净目录 `npm install <tarball>` 静默 exit 1（npm 对 `ccski: link:../ccski` 外部安装崩溃）——已在 README CLI 节如实记录，消除/打入归 4.8 发布清单。门禁：462/462（54 files）、typecheck 0、fmt 473 clean、diff-check clean、openspec 9/9（webui check/build 于 4.2 同树绿，本轮仅 md/spec 变更）。
- [x] 4.4 完成真实 CLI/daemon/RPC/file-system smoke and release checklist。
  - Files: `scripts/steward-smoke.sh.ts`, `docs/release/skill-steward.md`.
  - Acceptance: clean-directory start/stop/restart, workspace mutation, stale conflict, approval, rollback and DSH unavailable are evidenced.
  - Evidence: `scripts/steward-smoke.sh.ts` 重写为发布 smoke（取代 agent-steward 1.9 的 fixture-only 版，旧证据已归档）：真实 `bootDaemon`（干净 /tmp 短前缀隔离 home，显式 webuiDir=webui/build）×8 场景全部断言通过并落盘 `artifacts/steward-smoke.json`——clean-directory start（health + DSH mounted 149 entries）/ workspace mutation（import+create 落盘 sha256）/ stale conflict（外部编辑 → validate=stale + approve 类型化拒绝文案入证据）/ approval（重跑 → valid 逐项 checks → grant + fingerprint）/ apply（applied + mutation 明细 + 前后 sha256）/ rollback（edit 类 reverse proposal 独立审批后 apply，字节恢复快照 sha256 断言）/ stop→endpoint 拒连→同 home restart（registry 持久化 + 技能重发现 + 新端口 health）/ DSH unavailable（DSH_HOME=文件：mounted:false + typed reason + SPA 200 无 `__DSH_BOOT__`）。连续 3 次运行 exit 0。`docs/release/skill-steward.md` 发布清单：smoke 命令与场景断言表、门禁清单、生产包命令实测流程、已知 clean-install 阻塞（npm 对 `ccski: link:` spec 静默崩溃的复现与处置归 4.8）、发布证据索引。实测教训入清单（/tmp 短前缀 sun_path、源码态显式 webuiDir、try 必须带 catch、edit/split rollback 双形态）。门禁：462/462（54 files）、typecheck 0、webui check 0/0、build、fmt 475 clean、diff-check clean、openspec 9/9。
- [x] 4.5 运行全量门禁：`pnpm test`、`pnpm typecheck`、`pnpm --dir webui check`、`pnpm build`、`pnpm exec vp fmt --check`、`git diff --check`、`openspec validate --all --strict`。
  - Status: 4.5 串行执行于 c07545f + fmt 修正树：`pnpm test` 462/462（55 files）；`pnpm typecheck` 0 错；`pnpm --dir webui check` 0/0；`pnpm build` 成功（core bundle + webui staged 7 entries）；`pnpm exec vp fmt --check` 475 files clean（修正后）；`git diff --check` clean；`openspec validate --all --strict` 9/9。修正记录：4.4 提交时的 fmt 证据不准确——当时 `vp fmt` 被 && 链中更早的 typecheck 失败短路未执行，`--check` 实际在 `steward-smoke.sh.ts`/`steward-smoke.json` 两文件失败；本任务补跑 `vp fmt` 修正并复验（smoke 重跑 exit 0、全量 462/462、typecheck 0）。
- [x] 4.6 完成三个任务、四类 action 的真实端到端验收。
  - Files: `scripts/steward-smoke.sh.ts`, 本 change 的 `artifacts/acceptance.md`。
  - Cases: check 单 skill；check 多 skill 关系；optimize edit；organize disable/split/merge；分别记录 DSH session/tool-call id、Manager run/snapshot/proposal/audit id 和 Provider 内容/启停/资源树。
  - Acceptance: fixture 覆盖全部 action 和故障；真实锁定 DSH packages + 真实模型至少完成一次分析到方案、批准、apply、rollback。凭证缺失只记录 blocker，不能把 unavailable 作为通过。
  - Evidence: 新增 `scripts/steward-acceptance.sh.ts`（真实 bootDaemon + DSH host mounted + 生产 session-binder 接线，连续两次 exit 0；证据 `artifacts/steward-acceptance.json` + 矩阵总结 `artifacts/acceptance.md`）——六 case 全链（run→validate→approve→apply→rollback）各记录 snapshotId/dshSessionId（session-1…6）/toolCalls/proposalId/validation checks 计数/grantId/auditId/apply 终态/rollback 形态（edit+disable=reverse-proposal，split+merge=grant-replay）/apply 后与回滚后完整 Provider 文件树：check 单 skill（0 提案 2 工具）、check 多 skill（stream 帧实证 `skills.list_context`+`skills.relations`）、optimize edit、organize disable（`.SKILL.md`↔恢复）、organize split（`-plan/`+`-exec/`+源禁用→恢复）、organize merge（合并目录 SKILL.md+双源资产 copy+双源禁用目录保留→恢复）。fixture 补齐 action 面：scenario enum + `buildDeterministicDisableProposal`/`buildDeterministicMergeProposal`（merge 需 ≥2 技能，单技能 fail-closed；runtime 回归 +2=72/72）。故障矩阵：malformed（failed 终态 0 提案）、approval-replay（二次 approve 类型化拒绝）、stale（4.4 smoke 证据）、disconnect/cancel/late-event（runtime 既有测试）。真实模型 blocker 按条款类型化记录：`dsh.settings.update({preset:"live"})` → `PRESET_REQUIRES_CREDENTIAL`（本机无凭据；deterministic 四 action 全链已证，live 全链待凭据就绪补验，不以 unavailable 冒充通过）。门禁：464/464（55 files）、typecheck 0、webui check 0/0、build、fmt 478 clean（逐条独立执行）、diff-check clean、openspec 9/9。
- [x] 4.7 建立可解释的优化前后评估，不使用单一健康分。
  - Files: `test/fixtures/steward/evaluation/`, `artifacts/effectiveness.md`。
  - Cases: 明确该触发的 5 条输入、不该触发的 5 条输入；含冲突对、分工重叠对、配套脚本技能；保存 baseline 和 candidate 的原始结果、模型版本与判断理由。
  - Acceptance: 文档/资源完整性不退化；展示触发命中和误触发、任务断言、token 估算与真实消耗的区别；未知使用历史显示 unknown。每次优化有预期收益、证据、失败风险和保留/拒绝理由，不能把缩短字数视为默认提升。
  - Evidence: `test/fixtures/steward/evaluation/` 10 条输入 fixture（5 应触发：冲突对×2（共享触发词/互斥规则）、分工重叠对（同名）、文档缺陷（缺 description）、配套脚本技能（共享脚本+触发）；5 不应触发：分工清晰对、健康单技能、配套脚本各归其主、仅 info 宽触发面、仅 info 描述重叠；每条 expectation.json 携带期望/类别/判断理由/usage=unknown）。`scripts/steward-effectiveness.sh.ts`（与 `skills.relations` 同一条真实 analyzeDocuments 代码路径；连续两次 exit 0；证据 `artifacts/steward-effectiveness.json`）双策略对比：baseline=任何 finding 即提案（5 hit/**2 false-alarm**）；candidate=warning+ 且结构化类别才提案、info 级进人工复核（**5 hit / 0 false-alarm / 5 quiet**，09/10 的 info findings 转复核清单）。逐条记录原始 findings（kind@severity/skillIds/message）、判定（hit/miss/false-alarm/quiet）与 fixture 判断理由；token 估算（chars/4 上界）与真实消耗（deterministic=0 LLM token）分列；usage 全部 unknown；每 case 记录全部 SKILL.md sha256（评估只读，完整性零变更）。`artifacts/effectiveness.md`：结果矩阵 + 逐优化判定（info 门：预期收益=消除 2 误报、失败风险=真重叠延迟到人工复核（有 UI 复核面补偿）、判定=保留；明确拒绝「相似度自动 merge」并给理由；字数类指标不进判定）。回归固化 `test/steward-effectiveness.test.ts` 3/3（应触发全命中含期望 kind、不应触发零提案且 findings 与声明一致、baseline 误报恰为 09/10 两例）。门禁：467/467（56 files）、typecheck 0、webui check 0/0、fmt 509 clean、diff-check clean、openspec 9/9。
- [x] 4.8 完成实际 package clean-install 验证。
  - Files: `package.json`, build/stage scripts, `artifacts/clean-install.md`。
  - Steps: 消除生产 link:../ccski 或把其必要代码依法打入产物；锁定 DSH runtime 支持的 Node 版本；根 typecheck/build/test 覆盖 DSH plugin；pack 后在仓库外的空目录安装并启动。
  - Acceptance: 无本地源码、/tmp、未发布 private package 或 fixture 依赖；官方 packages/client assets 实际可加载；检查许可信息。不要仅凭 build 或 pack --dry-run 宣称可安装。
  - Evidence: 新增 `scripts/clean-install-check.sh.ts`（npm pack → 仓库外空目录 `npm install <tarball>` → 真实 node 黑盒驱动 start/status --json/stop/restart + HTTP 探针；证据 `artifacts/clean-install.json` + 根因记录 `artifacts/clean-install.md`）：install exit 0（旧 `ccski: link:` 静默崩溃点），ccski 连同 debug@4.4.3（均 MIT）打入 `dist/daemon.js`（`build-core` 改为显式 externals=dependencies−{ccski} + createRequire banner；link: 移 devDependencies 仅作 bundling 源），安装树无 ccski 泄漏/无 import 残留；runtime @deepseek-ai 11 包（dsh-app-boot/dsh-base/dsh-web-app/host-_/client-_/credentials*/cordis-plugin-loader）自 devDependencies 归位 dependencies；Manager DSH client plugin vendor 至 `dist/dsh-client`（含 LICENSE）并在安装态链接进 cordis loader 的实际解析路径（loader 以自身模块位置 parent-walk——pnpm 虚拟 store 天然命中根 node_modules，npm 布局需消费者 node_modules 链接；`dsh-official-profile` repoRoot 改为 bundle 感知，修正 dist 上溯三级指向包外的旧错误）；`@deepseek-ai/dsh-agent-tool-presentation` 0.1.0-rc.6→0.1.2-rc.1（消除 npm 下 dsh-invariants 0.1.0-rc.8/0.1.2-rc.1 双实例分裂，仅 ptc preset row 运行时引用）；DSH mounted 149 entries 含 `@skill-creator/dsh-client`（activation 同证）；HTTP：/api/health ok、/ =401（DSH 代理应答非 SPA 恢复面）、/manager/dsh-island.js 200、DSH 端口存活；stop→endpoint 释放→restart DSH 重挂载→stop 全 exit 0；Node 锁定 `engines.node >=22.13.0`（DSH code-runtime 需 node:module stripTypeScriptTypes，Node 22.13.0 引入；验证于 v24.20.0/npm 11.19.0，96 tarball entries；drill 曾误用 Bun 作 node 制造假缺陷，已固定真实 node 并以实测版本入证据）；license 清单：skill-creator/ccski/dsh-base/cordis 均 MIT。根 typecheck 覆盖 vendored plugin d.ts（tsconfig include packages/）。CLI `status` 增 DSH 健康行 + `--json` 完整输出（安装态取证面）。README/docs 发布清单同步（阻塞项改已解除）。门禁：467/467（56 files）、typecheck 0、webui check 0/0、build、fmt 512 clean、diff-check clean、openspec 9/9。
- [x] 4.9 修复复核发现的 permission capability、terminal polling 和 stale projection。
  - Cases: permission capability 必须由实际 handler/restriction 决定；选中终态 run 停止高频刷新；断线错误可见；切 route 后迟到 response 不覆盖新 scope；不用 as never 强行接受 UI 输入。
  - Acceptance: 延迟/失败 RPC 的组件交互测试和真实 1100px/680px 浏览器证据；检查键盘、长路径、空态、错误态和恢复动作，截图不能替代点击后的文件验证。
  - Evidence: 四项修复——(1) permission capability 由实际 handler 决定：codex-adapter `permissionRequests: false`（onRequest 对授权类 server request 统一 respondError，用户永远收不到可裁决请求）、pipeline-service 快照同改 false（无 permission handler）、fixture 保持 true（sink.permission 真实等待裁决）并注释依据；StewardView backend 卡片新增 no runtime approvals/no cancellation 真实 restriction 徽章、Cancel 按 run.capabilities.cancellation 禁用。(2) 终态停轮询：$effect 仅 selectedStatus==="running" 时挂 1s interval（primitive derived 防同值刷新重挂），终态/状态翻转即清理；真实浏览器 WS send 计数证据——终态 run 选中仅 2 次发送（1 events + 1 runs 刷新）、其后 6s+16s 零新增（唯一周期发送为连接层 `{"type":"ping"}` keepalive，非轮询）。(3) 断线可见：连续 ≥3 次轮询失败上浮 role=alert 横幅（成功即清）；daemon 停机真实截图「Cannot authenticate with the local daemon」（steward-4.9-disconnected-error-state.png），重启+新 token reload 恢复（skills 重列）。(4) scope guard：pollOnce 每次 await 后校验 selectedRunId 未变，切 run 迟到响应丢弃（组件测试断言 late-event-for-A 不入 B scope）。as never 清除：StewardView/StewardWorkflowView/store 签名改 branded（StewardBackendId/ProposalId/StewardProposalId/StewardAuditId），测试 fixture 走真实 Schema.parse。组件交互测试（新增 `webui/src/lib/__tests__/steward-view.test.ts`，@vitest-environment jsdom + svelte client 入口直连 helper + lucide 图标替身）：7/7 覆盖 restriction 展示/终态停轮询/翻转即停/失败横幅阈值与恢复/迟到响应丢弃/no-cancellation 禁用/typed approve。浏览器证据（真实 dev daemon + DSH 149 entries + 真 codex backend）：键盘 Tab 链（radio→Start run→palette，截图）、长路径（111 字符 workspace 导入 + 长 skill 名列表）、空态/错误态/恢复、1100px/680px 强制容器 scrollWidth===clientWidth 无溢出、全程 0 JS 错误；apply 文件验证——evidence-skill SKILL.md sha256 d2c60888…→7f9e801a…，steward-optimized 标记落盘，UI diff 与磁盘一致。浏览器点击实测额外暴露并修复 daemon 真实缺陷：pipeline sink.get 恒 null 使确定性 validate 步骤 NOT_FOUND（提案已铸但 terminal=failed 误导）——改为 run 内 Map，runtime 测试补 terminal=completed 回归钉。门禁：474/474（57 files）、typecheck 0、webui check 0/0、build、fmt 514 clean、diff-check clean、openspec 9/9。环境注记：全量门禁期间本机存在外部 worktree 的 vite build/dev（load ~10），期间 cli-lifecycle/dsh-profile 偶发超时抖动均可单跑复绿；清理了早前会话泄漏的 35 个 dsh-manager-island-live 进程后全量绿。
