# Tasks

- [x] 2.1 index `duplicates()` 投影（分组/冻结排序/成员形状）+ 单测
- [x] 2.2 契约 + RPC `skills.duplicates` + capability 登记（MCP 自动投影
      验证）+ service 编排（共用 freshen）
- [x] 2.3 webui store（loadSkillDuplicates 代次门 + 状态）+ WorkspacesHome
      区块（组行/跳转/空态/错误态）+ 单测
- [ ] 2.4 验证门 + 走查（区块真实渲染与跳转）
