{
  "name": "@jixoai/search",
  "version": "0.0.1",
  "private": true,
  "description": "General-purpose inverted-recall + frozen JS BM25 scoring search kernel (tantivy default, sqlite FTS5 fallback) — incubating private inside skill-creator; native tantivy binding is host-runtime-owned (root dependencies), NOT bundled here; pure domain library, no daemon/UI dependencies.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "pnpm --dir ../.. exec vitest run packages/search"
  },
  "dependencies": {
    "zod": "^4.4.3"
  }
}
