{
  "name": "@jixoai/search",
  "version": "0.0.1",
  "private": true,
  "description": "General-purpose inverted-recall + frozen JS BM25 scoring search kernel (sqlite FTS5 now, tantivy next) — incubating inside skill-creator; pure domain library, no daemon/UI dependencies.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "pnpm --dir ../.. exec vitest run packages/search"
  },
  "dependencies": {
    "zod": "^4.4.3"
  }
}
